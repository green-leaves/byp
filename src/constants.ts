/**
 * Constants used throughout the application
 */

// Maximum chunk size in bytes (64MB)
export const MAX_CHUNK_SIZE = 64 * 1024 * 1024;

// Default package name for publishing/downloading chunked files
export const DEFAULT_PACKAGE_NAME = '@byp/packages';